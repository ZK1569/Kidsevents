{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{asset('css/Admin/style.css')}}">
{% endblock %}

{% block body %}
<body class='bodyImg'>
	<div class="text-box">
		<div class="center">
            {{ form_start(form, { attr : { 'novalidate' : 'novalidate' } }) }}
                <h2>{{form_widget(form.mail) }} est bien l'adresse à laquelle vous souhaitez recevoir nos actualités ? </br> </h2>
                    <p>
                        {{ form_widget(form.interet) }}
                    </p>
	            <input type="submit" class="btn btn-primary">
            {{ form_end(form) }} </br>
        </div>
	</div>
{% endblock %}