{% extends 'base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{asset('css/Login/registerStyle.css')}}">
{% endblock %}

{% block title %}Inscription
{% endblock %}

{# {% block body %}
    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.firstname) }}
        {{ form_row(registrationForm.lastname) }}
        {{ form_row(registrationForm.phone) }}
        {{ form_row(registrationForm.address) }}

        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %} #}

{% block body %}

	<div class="container">
		<div class="title">
			<p>Inscription</p>
		</div>
		{{ form_start(registrationForm) }}

		<div class="user-details">
			<div class="input-box">
				{{  form_label(registrationForm.firstname, 'Votre prénom :', { label_attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.firstname) }}</div> 
				{{  form_widget(registrationForm.firstname) }}
			</div>

            <div class="input-box">
				{{  form_label(registrationForm.lastname, 'Votre nom :', { label_attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.lastname) }}</div> 
				{{  form_widget(registrationForm.lastname) }}
			</div>

            <div class="input-box">
				{{  form_label(registrationForm.email, 'Email :', { label_attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.email) }}</div> 
				{{  form_widget(registrationForm.email) }}
			</div>

            <div class="input-box">
				{{  form_label(registrationForm.phone, 'Numero de téléphone :', { label_attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.phone) }}</div> 
				{{  form_widget(registrationForm.phone) }}
			</div>

            <div class="input-box">
				{{ form_label(registrationForm.plainPassword, 'Mot de passe :', { label_attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.plainPassword) }}</div> 
				{{ form_widget(registrationForm.plainPassword) }}
			</div>

            <div class="input-box">
				{{  form_label(registrationForm.address, 'Adresse :', { attr: { class: "details" } }) }}
				<div class="form-errors">{{ form_errors(registrationForm.address) }}</div> 
				{{  form_widget(registrationForm.address) }}
			</div>
		</div>
        <div class="button">
            <input type="submit" value="S'inscrire">
        </div>
		{{ form_end(registrationForm) }}
	</div>

{% endblock %}
