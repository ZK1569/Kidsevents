{# ---- Box slider -------------- #}
<div class="all-slider">

    

    <div class="box">
        {# ---- img-box ----------- #}
        <div class="slider-img">
        
            <img src="{{asset('img/Themes/' ~ product.mainPicture)}}" alt="{{product.name}}">
            {# --- Overlayer ------------ #}
            <div class="overlay">
                {# --- buy button -----------#}
                <a href="{{url ('product_show', {'slug':product.slug})}}" class="check-btn">Consulter le theme</a>
            </div>
        </div>

        {# --- detail-box ------ #}
        <div class="detail-box">
            {{product.name | upper}}            
            {# --- price ---------#}
            <a href="#" class="price">{{product.price}} €</a>


        </div>
    </div>
</div> 