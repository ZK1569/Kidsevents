{# ---- Box slider -------------- #}
<div class="all-slider">

    

    <div class="box">
        {# ---- img-box ----------- #}
        <div class="slider-img">
        
            <img src="{{asset('img/supplement/' ~ supplement.mainPicture)}}" alt="{{supplement.name}}">
            {# --- Overlayer ------------ #}
            <div class="overlay">
                {# --- type --------- #}
                <div class="type">
                    <a href="{{url ('cart_add_supplement', {'slug':supplement.slug} )}}" class="check-btn">ajouter au panier</a>
                </div>
            </div>
        </div>

        {# --- detail-box ------ #}
        <div class="detail-box">
            <a href="{{url ('supplement_show', {'slug':supplement.slug} )}}" class="name">{{supplement.name | upper}}</a>
            {# --- price ---------#}
            <a href="#" class="price">{{supplement.price}} €</a>


        </div>
    </div>
</div> 